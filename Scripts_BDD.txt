Base gestion commerciale

CREATE TABLE demandeDeFormation (
  idDemandeFormation integer GENERATED BY DEFAULT AS IDENTITY, 
  nomClient          varchar(50), 
  dateDeDemande      date, 
  codeFormation      varchar(255), 
  intituleFormation  varchar(255), 
  codeClient         integer, 
  PRIMARY KEY (idDemandeFormation));


Base gestion formation

CREATE TABLE formation (
  idFormation         integer GENERATED BY DEFAULT AS IDENTITY, 
  keySalle            integer, 
  keyFormateur        integer, 
  dateFormation       date, 
  demandeFormationKey integer NOT NULL, 
  statut              varchar(100), 
  PRIMARY KEY (idFormation));
CREATE TABLE formationCompose (
  formationKey        integer NOT NULL, 
  demandeFormationKey integer NOT NULL, 
  nbParticipants      integer, 
  PRIMARY KEY (formationKey, 
  demandeFormationKey));
CREATE TABLE stockageDemandeFormation (
  idDemandeFormation     integer GENERATED BY DEFAULT AS IDENTITY, 
  dateDemandeFormation   date, 
  codeFormationCatalogue varchar(100), 
  intituleFormation      varchar(255), 
  codeClient             integer, 
  nbPersonne             integer, 
  nomClient              varchar(100), 
  PRIMARY KEY (idDemandeFormation));
ALTER TABLE formationCompose ADD CONSTRAINT FKformationC291941 FOREIGN KEY (formationKey) REFERENCES formation (idFormation);
ALTER TABLE formationCompose ADD CONSTRAINT FKformationC386288 FOREIGN KEY (demandeFormationKey) REFERENCES stockageDemandeFormation (idDemandeFormation);


Base gestion rh

CREATE TABLE calendrier (
  idCalendrier   integer GENERATED BY DEFAULT AS IDENTITY, 
  dateCalendrier date, 
  PRIMARY KEY (idCalendrier));
  
CREATE TABLE calendrier_formateur (
  calendrierKey integer NOT NULL, 
  formateurKey  integer NOT NULL, 
  statut        varchar(50), 
  PRIMARY KEY (calendrierKey, 
  formateurKey));
  
CREATE TABLE competence (
  idCompetence  integer GENERATED BY DEFAULT AS IDENTITY, 
  nomCompetence varchar(100), 
  PRIMARY KEY (idCompetence));
  
CREATE TABLE competence_formateur (
  formateurKey  integer NOT NULL, 
  competenceKey integer NOT NULL, 
  PRIMARY KEY (formateurKey, 
  competenceKey));
  
CREATE TABLE formateur (
  idFormateur integer GENERATED BY DEFAULT AS IDENTITY, 
  nom         varchar(100), 
  prenom      varchar(100), 
  PRIMARY KEY (idFormateur));
  
ALTER TABLE competence_formateur ADD CONSTRAINT FKcompetence420398 FOREIGN KEY (competenceKey) REFERENCES competence (idCompetence);
ALTER TABLE competence_formateur ADD CONSTRAINT FKcompetence414704 FOREIGN KEY (formateurKey) REFERENCES formateur (idFormateur);
ALTER TABLE calendrier_formateur ADD CONSTRAINT FKcalendrier269605 FOREIGN KEY (calendrierKey) REFERENCES calendrier (idCalendrier);
ALTER TABLE calendrier_formateur ADD CONSTRAINT FKcalendrier144012 FOREIGN KEY (formateurKey) REFERENCES formateur (idFormateur);

Base gestion patrimoine

CREATE TABLE calendrier (
  idCalendrier   integer GENERATED BY DEFAULT AS IDENTITY, 
  dateCalendrier date, 
  PRIMARY KEY (idCalendrier));
  
CREATE TABLE calendrier_salle (
  calendrierKey integer NOT NULL, 
  salleKey      integer NOT NULL, 
  statut        varchar(50), 
  PRIMARY KEY (calendrierKey, 
  salleKey));
  
CREATE TABLE equipement (
  idEquipement  integer GENERATED BY DEFAULT AS IDENTITY, 
  nomEquipement varchar(255), 
  PRIMARY KEY (idEquipement));
  
CREATE TABLE salle (
  idSalle integer GENERATED BY DEFAULT AS IDENTITY, 
  nom     varchar(50), 
  PRIMARY KEY (idSalle));
  
CREATE TABLE salle_equipement (
  salleKey      integer NOT NULL, 
  equipementKey integer NOT NULL, 
  PRIMARY KEY (salleKey, 
  equipementKey));
  
ALTER TABLE salle_equipement ADD CONSTRAINT FKsalle_equi574014 FOREIGN KEY (salleKey) REFERENCES salle (idSalle);
ALTER TABLE salle_equipement ADD CONSTRAINT FKsalle_equi824723 FOREIGN KEY (equipementKey) REFERENCES equipement (idEquipement);
ALTER TABLE calendrier_salle ADD CONSTRAINT FKcalendrier333540 FOREIGN KEY (calendrierKey) REFERENCES calendrier (idCalendrier);
ALTER TABLE calendrier_salle ADD CONSTRAINT FKcalendrier694111 FOREIGN KEY (salleKey) REFERENCES salle (idSalle);

gestion technico commerciale

CREATE TABLE formateurCompetent (
  idFormateurCompetent integer GENERATED BY DEFAULT AS IDENTITY, 
  formateurKey         integer NOT NULL, 
  PRIMARY KEY (idFormateurCompetent));
  
CREATE TABLE formationCatalogue (
  idFormationCatalogue integer GENERATED BY DEFAULT AS IDENTITY, 
  code                 varchar(10), 
  intitule             varchar(50), 
  niveau               varchar(255), 
  typeDuree            varchar(100), 
  capaciteMin          integer, 
  capaciteMax          integer, 
  tarifForfaitaire     double, 
  PRIMARY KEY (idFormationCatalogue));
  
CREATE TABLE formationCatalogue_formateur (
  formationCatalogueKey integer NOT NULL, 
  formateurCompetentKey integer NOT NULL, 
  PRIMARY KEY (formationCatalogueKey, 
  formateurCompetentKey));
  
CREATE TABLE formationCatalogue_salleAdequate (
  formationCatalogueKey integer NOT NULL, 
  salleAdequateKey      integer NOT NULL, 
  PRIMARY KEY (formationCatalogueKey, 
  salleAdequateKey));
  
CREATE TABLE salleAdequate (
  idSalleAdequat integer GENERATED BY DEFAULT AS IDENTITY, 
  salleKey       integer NOT NULL, 
  PRIMARY KEY (idSalleAdequat));
  
ALTER TABLE formationCatalogue_formateur ADD CONSTRAINT FKformationC332187 FOREIGN KEY (formationCatalogueKey) REFERENCES formationCatalogue (idFormationCatalogue);
ALTER TABLE formationCatalogue_formateur ADD CONSTRAINT FKformationC455135 FOREIGN KEY (formateurCompetentKey) REFERENCES formateurCompetent (idFormateurCompetent);
ALTER TABLE formationCatalogue_salleAdequate ADD CONSTRAINT FKformationC842109 FOREIGN KEY (formationCatalogueKey) REFERENCES formationCatalogue (idFormationCatalogue);
ALTER TABLE formationCatalogue_salleAdequate ADD CONSTRAINT FKformationC344248 FOREIGN KEY (salleAdequateKey) REFERENCES salleAdequate (idSalleAdequat);



